name: dayforge
description: "A private cross-platform daily log."
publish_to: 'none'
version: 0.1.3+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
